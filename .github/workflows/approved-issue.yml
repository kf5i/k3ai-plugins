name: Approved Labels

on: [issue_comment]
env:
  MY_GITHUB_TOKEN: ${{ secrets.MY_GITHUB_TOKEN }}
  
jobs:
  remove_labels:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions-ecosystem/action-remove-labels@v1
        if: ${{ startsWith(github.event.comment.body, '/approved') }}
        with:
          github_token: ${{ secrets.MY_GITHUB_TOKEN }}
          labels: |
            needs-triage
      - uses: actions/checkout@v2
      - uses: actions-ecosystem/action-add-labels@v1
        if: ${{ startsWith(github.event.comment.body, '/approved') }}
        with:
          github_token: ${{ secrets.MY_GITHUB_TOKEN }} 
          labels: |
            approved
      - uses: actions/checkout@v2
      - uses: actions-ecosystem/action-remove-labels@v1
        if: ${{ startsWith(github.event.comment.body, '/roadmap') }}
        with:
          github_token: ${{ secrets.MY_GITHUB_TOKEN }}
          labels: |
            needs-triage
      - uses: actions/checkout@v2
      - uses: actions-ecosystem/action-add-labels@v1
        if: ${{ startsWith(github.event.comment.body, '/roadmap') }}
        with:
          github_token: ${{ secrets.MY_GITHUB_TOKEN }}
          labels: |
            roadmap
      - uses: srggrs/assign-one-project-github-action@1.2.0
        if: |
         ${{ startsWith(github.event.comment.body, '/approved') }}
         ${{ startsWith(github.event.comment.body, '/roadmap') }}
        with:
          project: 'https://github.com/orgs/kf5i/projects/2'
          column_name: 'Approved'

